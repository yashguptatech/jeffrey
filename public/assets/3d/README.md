# 3D Asset Pipeline

This directory stores production-ready 3D storytelling assets.

- `models/`: GLB/GLTF character, map, and prop assets
- `textures/`: compressed textures (prefer KTX2/WEBP)
- `environments/`: HDRI or baked environment maps

Use optimized placeholder assets during development and swap in high-fidelity assets when approved.
